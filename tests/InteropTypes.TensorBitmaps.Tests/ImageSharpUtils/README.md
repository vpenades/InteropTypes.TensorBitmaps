
### ImageSharp usage

Due to licensing constraints, I can't expose ImageSharp as a wrapped library and I limit its usage for unit testing only.

Also, I am deliverately using an outdated version of ImageSharp which is the last one that does not require registration.